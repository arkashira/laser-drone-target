[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"

[tool.poetry]
name = "drone-targeting"
version = "1.0.0"
description = "Drone targeting tool"

[tool.poetry.dependencies]
python = "^3.8"
